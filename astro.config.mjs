// @ts-check
import { defineConfig } from 'astro/config';

// GitHub Pages user site: served from the domain root, so no `base`.
export default defineConfig({
  site: 'https://greggocabral.github.io',
});
